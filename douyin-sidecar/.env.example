HOST=0.0.0.0
PORT=8080
DOUYIN_SIDECAR_TOKEN=replace-with-sidecar-token
# mock is local-only. Production signoff requires webhook_proxy with a verified upstream endpoint.
DOUYIN_DRIVER_MODE=mock
DOUYIN_UPSTREAM_URL=
DOUYIN_UPSTREAM_TOKEN=
